<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="com.example.medihelper.AppDateTime" />

        <variable
            name="displayData"
            type="com.example.medihelper.mainapp.medicineplanlist.MedicinePlanHistoryViewModel.HistoryItemDisplayData" />
    </data>

    <LinearLayout xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="4dp"
        android:paddingEnd="4dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{AppDateTime.dayOfWeekString(displayData.plannedDate), default=dzień}"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Subtitle1" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{AppDateTime.dayMonthString(displayData.plannedDate), default=Data}"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Subtitle2"
            android:textColor="@color/colorTextTertiary"/>

        <LinearLayout
            android:id="@+id/lay_history_checkboxes"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="16dp">

            <include layout="@layout/view_history_checkbox" />

            <include layout="@layout/view_history_checkbox" />

            <include layout="@layout/view_history_checkbox" />
        </LinearLayout>

    </LinearLayout>
</layout>